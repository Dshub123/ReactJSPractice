import React from 'react'

const FirstFunctionComponent = (props) => {
    return (
        <div>
            Hello, {props.name}! I am a FirstFunctionComponent.

        </div>
    )
}

export default FirstFunctionComponent
